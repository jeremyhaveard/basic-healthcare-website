# Reserve browser-visible configuration for approved, non-sensitive integrations.
# Never add secrets, API keys, tokens, patient data, or production credentials here.
# NEXT_PUBLIC_EXAMPLE_CONFIGURATION=